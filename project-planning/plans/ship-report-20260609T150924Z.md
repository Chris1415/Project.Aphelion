# Ship Report

---
document_type: ship_report
artifact_name: ship-report-20260609T150924Z.md
generated_at: 2026-06-10T17:05:00Z
run_manifest: project-planning/workflow/run-20260609T150924Z.json
prd_id: PRD-000
status: shipped
work_item_type: prd
source_inputs:
  - project-planning/PRD/prd-000.md
  - project-planning/PRD/prd-minimal-000.md
  - project-planning/architecture/architecture-20260609T150924Z.md
  - project-planning/plans/task-breakdown-20260609T150924Z.md
  - project-planning/plans/code-review-20260609T150924Z.md
  - project-planning/plans/test-report-20260609T150924Z.md
  - project-planning/sitecore-content-model.md
---

## 1. Summary

PRD-000 delivers **Acts 1–2** of the Aphelion Content SDK NEW-site build (a dogfood of the redesigned static-app→port flow): a standalone, plain-React **static reference app** (`static/`) realizing the operator-selected **v5b "Flux"** design across five marketing routes, plus a mechanically-derived **`sitecore-content-model.md`** (the operator's Act-2 CMS runbook). The app is Next 16 / React 19 / Tailwind v4 App Router with **zero `@sitecore-*` dependencies**, ships first-class dark+light+system theming, and passes its full quality bar: 100 Vitest + 85 Playwright green, 20 screenshot-diff fidelity baselines, axe a11y clean on all 10 route×theme combos, and zero horizontal scroll at 375/768/1280. The head-app scaffold + SDK port + deploy are intentionally **PRD-001 (Act 3)**.

## 2. Delivered vs Specified

| Spec item (user story) | Status | Evidence | Notes |
|------------------------|--------|----------|-------|
| **US-1** Faithful static site (5 routes, screenshot-diff clean, zero `@sitecore`) | delivered | `test-report:Automated Checks` (85 Playwright, 20 baselines ≤5%); `test-report:Findings` (zero-SDK invariant T034) | All 5 routes prerender; built from POC markup |
| **US-2** 3-state theming (light/dark/system, no FOUC, no hydration warning) | delivered | `test-report:E2E` (T012 theme probe, 8 tests); ADR-0004 | `useSyncExternalStore` + preflight; T013 GATE passed |
| **US-3** Sitecore-portable components (flat leaves / folder-of-children) | delivered | `sitecore-content-model.md` (11 leaf-flat + 6 folder-of-children); `code-review:architecture` (ADR-0005 honored) | Nav excluded; `limit` = parameter |
| **US-4** Operator CMS runbook (dependency-ordered, exact paths, verbatim values) | delivered | `manual:sitecore-content-model.md` (12-step checklist, 31 content items verbatim) | 3 net-new items flagged for PRD-001 |
| **US-5** Representative marketing content (real copy, royalty-free imagery, tasteful motion) | delivered | `manual:test-report Visual Testing`; `code-review:security` (imagery royalty-free) | Reduced-motion safe; Unsplash (Unsplash License) |
| **§12 ship gate** build + lint + tsc green | delivered | `test-report:Automated Checks` | 6 routes, clean |
| **§12 ship gate** Vitest component/theme suite | delivered | `test-report:Automated Checks` (100/100) | |
| **§12 ship gate** screenshot-diff fidelity (both themes, 390+1440, 5 routes) | delivered | `test-report:E2E` (T033, 20 baselines ≤5%) | Final Home visual vs POC = operator eyeball (non-blocking) |
| **§12 ship gate** a11y / contrast AA both themes | delivered | `test-report:E2E` (axe 0 serious/critical; 4 runtime contrast ratios ≥4.5:1) | |
| **§12 ship gate** `sitecore-content-model.md` complete | delivered | `manual:sitecore-content-model.md` | |

**All in-scope requirements delivered.** No partial / cut items.

## 3. Deferred Items

These were explicitly **out of PRD-000 scope** (not partial delivery) — they are PRD-001 / future work:

- **Act 3 — head-app scaffold + SDK port + deploy** → **PRD-001** (the natural two-PRD split, ADR-0002). Needs the operator's Act-2 CMS build + a live tenant + Edge contextId.
- **3 net-new content-model decisions** surfaced by E5, to resolve at PRD-001 kickoff: HeroMeta array (flatten to 6 fields?), ContactDetails (inline in route → needs its own rendering+datasource template), Marquee items sourcing (TBD).
- **i18n / personalization / real forms backend / search / commerce** → future PRDs (deferred-topics list).

## 4. Known Limitations

- **Final Home visual fidelity vs the POC is operator visual judgment** — the automated screenshot-diff passed structurally + ≤5% pixel delta, but the POC was Home-only so inner-route baselines are self-generated (regression value, not POC-diff). Non-blocking; recommend an eyeball pass.
- **Real imagery requires network** — Unsplash hotlinks have a CSS mesh fallback (`onError`), so offline degrades gracefully but isn't pixel-identical.
- **NFR-1 Lighthouse** is a soft target (not gated) — the gooey SVG `#gooey` filter + animated mesh have a repaint cost; `prefers-reduced-motion` freeze is the valve.
- **The static app is a reference deliverable, not deployed** (by design — deploy is Act 3).

## 5. Test Coverage Summary

- **Unit / component tests (Vitest):** 100 / 100 pass (8 files) — theme store, motion hooks, MobileNav, all leaf/container components (render-from-props), forms (never-fetch), zero-SDK invariant + mutation check, theme integration.
- **E2E tests (Playwright):** 85 / 85 pass (+1 intentional skip) — theme/hydration probe (T012), screenshot-diff fidelity (20 baselines), axe a11y (10 route×theme), 4 runtime contrast assertions, reduced-motion freeze, keyboard nav.
- **Manual verification:** 5 routes walked in both themes (no console errors, validation states, motion freeze); responsive at 375/768/1280 (zero h-scroll, 15/15).
- **Open test gaps:** none material for PRD-000 scope. Real-tenant / editor-preview coverage is PRD-001 (no tenant exists here).

## 6. Technical Debt Introduced

- **Inner-route screenshot baselines are self-generated** (POC was Home-only) — they catch regressions but aren't a POC-diff; acceptable, documented in § 4c-7.
- **`RichTextSection` renders trusted static HTML via React's raw-HTML prop** for the in-repo About body — verified non-user-input in code review (security category). When this becomes a Sitecore Rich Text field in Act 3, the SDK `<RichText>` field component replaces it.
- No TODO/FIXME shortcuts left in shipped code. Otherwise: none identified.

## 7. ADR Compliance

| ADR | Decision | Compliance | Notes |
|-----|----------|------------|-------|
| adr-0001 | Use ADRs as architecture backbone | compliant | 8 ADRs maintained |
| adr-0002 | Two-PRD split (Acts 1–2 vs Act 3) | compliant | PRD-000 scoped to Acts 1–2; Act 3 = PRD-001 |
| adr-0003 | Static-app-first (no head-app scaffold) | compliant | `static/` is plain `create-next-app`, zero SDK |
| adr-0004 | Theme: preflight + useSyncExternalStore + Tailwind v4 | compliant | T012/T013 hydration gate passed |
| adr-0005 | Content-model boundary (flat / folder-of-children / nav excluded) | compliant | 11 leaf-flat + 6 folder; nav excluded; `limit`=param |
| adr-0006 | Presentational-only forms | compliant | NewsletterCTA + ContactForm never `fetch` (asserted) |
| adr-0007 | Static-app stack/scaffold + `src/ui` vs `src/components` split | compliant | `create-next-app`; infra in `src/ui`+`src/lib` |
| adr-0008 | Test + fidelity-gate strategy (Vitest + Playwright) | compliant | 100 Vitest + 85 Playwright; `toHaveScreenshot` ≤5% |

## 8. Recommendations for Next Iteration

1. **Open PRD-001 (Act 3)** stacked off `prd-000`: operator builds the tenant from `sitecore-content-model.md` → scaffold the head app (`create-content-sdk-app`) against the live contextId → port each plain-prop component to SDK field components → deploy to Vercel. Resolve the 3 net-new content-model decisions at kickoff.
2. **Operator eyeball** the running app vs `poc-v5b-prd000/` in both themes before the PR merges (the one non-automated fidelity check).
3. Consider promoting Aphelion to a **standing reference** for future Content SDK new-site dogfoods (the static-app→port flow held up cleanly).

## 9. What Hurt This Run (Friction Log Summary)

No friction events captured this run (the friction-log nudge was default-skip at each stage). One process observation worth noting outside the friction log: a **session-limit interruption froze the v3/v4/v5 POC sub-agents mid-write** — recovered cleanly via `SendMessage` resume (context intact, only the unfinished `index.html`/`click-targets.md` remained). Resume-on-pause is a reliable pattern for long parallel sub-agent batches.

## Handoff Metadata
- Canonical run manifest: `project-planning/workflow/run-20260609T150924Z.json`
- PRD this report closes: PRD-000 (Acts 1–2)
- Final manifest status: shipped
- Deferred items count: 3 buckets (Act 3 / 3 net-new model decisions / future-topics)
- Known limitations count: 4
