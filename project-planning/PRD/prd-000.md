# Product Requirements Document

---
document_type: prd
prd_id: PRD-000
artifact_name: prd-000.md
generated_at: 2026-06-09T15:09:24Z
run_manifest: project-planning/workflow/run-20260609T150924Z.json
source_inputs:
  - prompt-only (dogfood request, 2026-06-09)
consumed_by:
  - Software Architect Agent
next_input:
  - project-planning/architecture/architecture-<timestamp>.md
---

<!-- Soft cap: ~400 lines. PRD-000 is scoped to Acts 1–2 of the Content SDK NEW-site flow (static reference app + content-model spec). Act 3 (head-app scaffold + SDK port + deploy against a live tenant) is PRD-001. -->

## 1. Executive Summary

**Aphelion** is a marketing website for a fictional premium commercial-spaceflight & cosmos-lifestyle brand, built as a **dogfood run of the Content SDK NEW-site pipeline**. The end state (across PRD-000 + PRD-001) is a Sitecore XM Cloud Content SDK head application (App Router) serving a five-route marketing site against a live tenant.

This PRD (**PRD-000**) covers **Acts 1–2** of the NEW-site flow only:
- **Act 1** — a standalone, **plain-React** Next.js "static app" that realizes the approved clickdummy as faithfully as possible, with **zero Sitecore dependencies**. This is the design-fidelity reference deliverable.
- **Act 2** — a mechanically derived **`sitecore-content-model.md`** spec: the operator's click-by-click CMS runbook to build + publish the tenant.

**Act 3** (scaffold the real head app, port plain props → SDK field components against live Edge, deploy to Vercel) is deliberately split into **PRD-001**, because it is tenant-gated and must not block this self-contained, tenant-free half (per the Last-Edit-Trail post-mortem: *don't bundle ship-ready work with tenant-gated work*).

## 1.5 Developer slice (extracted to prd-minimal-NNN.md)

<!-- PRD_MINIMAL_BEGIN -->

### Problem

We are dogfooding the redesigned Content SDK NEW-site build flow (static-app → port). To exercise it we need a real-feeling marketing site for a cosmos/space brand, **Aphelion**, built the canonical way: a faithful plain-React static app first, then a derived content-model spec the operator can build in Sitecore — no mock infrastructure, no premature head-app scaffolding.

### Goal

Deliver a polished, responsive, dark+light+system-themed **plain-React Next.js static app** (App Router, Tailwind v4, **zero `@sitecore-content-sdk/*`**) for the five Aphelion routes that matches the approved clickdummy under a screenshot-diff gate, plus a mechanically derived `sitecore-content-model.md` runbook. This is Act 1 + Act 2; the head-app port is PRD-001.

### Non-negotiables

- **Zero Sitecore dependencies in the static app.** No `@sitecore-content-sdk/*` packages, no field types, no SDK field components. Plain props only (`title: string`, `imageSrc: string`, `href: string`).
- **One flat prop object per LEAF component**, designed so each scalar prop maps 1:1 to a *future* Sitecore datasource field (this prop set IS the content model). `HeroBanner({ eyebrow, title, subtitle, image, primaryCta })`.
- **Array derivation rule (container/list components).** Array-of-child props (`destinations: Destination[]`, `stats: Stat[]`, etc. in `DestinationsGrid`/`ValueProps`/`ExperienceShowcase`/`StatsBand`/`Testimonials`) map to **child items under a folder datasource** — the canonical SXA Children-resolver pattern (`custom_content-sdk-resolver-patterns`): the container's datasource is a *folder*; each child is an item of the child template carrying the card fields; the container's own scalars (e.g. section heading) become fields on the folder item. NOT multilist, NOT a repeating field.
- **Build mode.** Standard Next.js production build — **NOT** `output: 'export'`/SSG (a static export would misrepresent the head-app SSR/proxy target it ports into). `npm run build` green is a gate.
- **Nav is the one content-model exception.** Header + footer navigation use a static hard-coded tree in the static app and are **excluded from content-model derivation** — they are sourced via the component-level navigation pattern in Act 3 (PRD-001). Do not flag nav as a flat-prop violation.
- **Dark + light + system theme** per `custom_content-sdk-theme-hydration-pattern`: Tailwind v4 `@theme` tokens, FOUC preflight via `public/theme-init.js` (NOT React inline raw-HTML script), `useSyncExternalStore`-based provider, mounted-flag for client-only styling. Theme code is written here and **ports verbatim** to the head app later.
- **No browser globals in render/SSR-init paths** (`typeof window`, `matchMedia`, `IntersectionObserver` belong in `useEffect`) — hydration safety; Playwright is the gate, vitest+build can't catch it.
- **Screenshot-diff fidelity gate** of every route in BOTH themes BEFORE operator review (build from the clickdummy's exact structure, not prose). **Definition:** tight **structural + visual** parity, NOT literal pixel-perfect. Baseline = the operator-selected clickdummy variant rendered in Playwright. Viewports **390 (mobile) + 1440 (desktop)**. Pass = structural parity (same sections, order, layout) + ≤~5% per-route pixel delta per theme.
- **Tasteful motion, not overloaded.** Effects must respect `prefers-reduced-motion`.
- **The content-model spec is derived mechanically** from the static app's component props (one prop → one field, SDK field type chosen by usage).

### In scope / out of scope

- **In scope:** static plain-React app for 5 routes (Home, Destinations, Experiences, About, Contact); full reusable component inventory; theme stack; responsive layouts; component/unit tests + Playwright screenshot-diff; `sitecore-content-model.md` derived spec.
- **Out of scope (→ PRD-001):** Content SDK head-app scaffold, SDK field-component port, live tenant connection, `client.getPage()` wiring, component-level navigation GraphQL, deploy. **Out of scope (deferred entirely):** commerce/cart, i18n/localization, personalization, real forms backend, search.

### Success criteria

- Static app at `products/head-application/aphelion/static/` runs via `npm run dev`, all 5 routes render with no console errors in both themes.
- Screenshot-diff of every route vs the selected clickdummy variant passes the fidelity definition above (structural parity + ≤~5% pixel delta) at 390 + 1440, light AND dark.
- Theme toggle: light / dark / system all work; no FOUC on hard reload; no hydration mismatch warnings.
- Every LEAF component exposes one flat plain-prop object mapping 1:1 to a single Sitecore field; every container/list component follows the array derivation rule (folder-of-child-items); nav is the documented exception; anything else unmappable is flagged net-new in the spec.
- `sitecore-content-model.md` exists: per-rendering table (componentName + datasource fields + types), per-page placeholder composition, content items with verbatim field values, publish targets — authored as a dependency-ordered operator checklist.
- `npm run build`, `npm run lint`, `npx tsc --noEmit` all green.

### Key constraints & assumptions

- App Router, Next.js (scaffold via `create-next-app`, pinned by the scaffold — do NOT hand-write `package.json`, per rule `50-scaffold`).
- Tailwind v4 (`@theme` token model). Hex-literal tokens must NOT be wrapped in `hsl()` (per `hsl_var_token_broken_with_hex_values`).
- Newsletter + Contact are **presentational only** (client-side validation/UI, no submit backend) in v1 — Assumption A1.
- `SITE_COLLECTION_NAME=cosmos`, `SITE_NAME=aphelion` (used by the content-model spec paths; no tenant work in this PRD).
- Imagery: use royalty-free/placeholder cosmos imagery or generated gradients/SVG; do not embed copyrighted brand assets.

### Handoff

- **Full PRD:** `project-planning/PRD/prd-000.md` (humans + upstream agents only — not loaded by agent 08 in normal flow).
- **Executable contract:** `project-planning/plans/task-breakdown-<timestamp>.md` after QA (07) enrichment.
- **Next command:** `/architect` (full track; UI Designer produces 2 clickdummy variants).

<!-- PRD_MINIMAL_END -->

## 2. Problem Statement

The Content SDK build flow was redesigned (2026-06-09) so the default NEW-site path builds a **plain-React static app first**, derives a content model from it, has the operator build the tenant, and only then ports into a head app against **live Edge data** — no mock infrastructure (the prior `stellar`/`dogfood-content-sdk` mock-first run was deleted for sprawl). We need a real dogfood to validate that the pipeline (`/create-prd` → `/architect` → `/task-breakdown` → `/implement` → `/code-review` → `/test` → `/ship`) reacts correctly to this flow, and to surface friction before it hits a real customer project. Aphelion is the vehicle: a marketing site rich enough to be representative (chrome, hero, content bands, card grids, promos, stats, testimonials, forms) but bounded enough to finish.

## 3. Goals and Success Metrics

| Goal | Metric |
|------|--------|
| Faithful design realization | Screenshot-diff vs selected clickdummy variant passes (structural parity + ≤~5% pixel delta), all 5 routes, 390 + 1440, both themes |
| Sitecore-portable component shapes | 100% of component props map 1:1 to a single derivable Sitecore field; 0 unmappable props (or all flagged net-new) |
| Theme correctness | 3-state theme works; 0 FOUC; 0 hydration warnings |
| Build health | `build` + `lint` + `tsc --noEmit` green; component tests pass |
| Operator-ready CMS runbook | `sitecore-content-model.md` is a complete dependency-ordered checklist the operator can execute without further questions |
| Pipeline learning (meta) | Friction log captures every desync/ambiguity for framework feedback |

**Non-metric (explicit):** real-tenant smoke is **not** a PRD-000 gate — there is no tenant yet. Fidelity is the gate here.

## 4. Target Users

- **Primary (build-time):** the operator dogfooding the pipeline + the downstream build agents (Architect, Lead Dev, QA, Developer) who consume this spec.
- **Represented (product narrative):** a prospective Aphelion customer — an affluent traveler browsing premium spaceflight "destinations" and "experiences," evaluating the brand. The static app must read like a site that user would trust.
- **Secondary:** the operator-as-CMS-author who will execute `sitecore-content-model.md` in Act 2.

## 5. Product Scope

### In Scope

- **Routes (5):** Home (`/`), Destinations (`/destinations`), Experiences (`/experiences`), About (`/about`), Contact (`/contact`).
- **Chrome:** `SiteHeader` (logo + primary nav + theme toggle; static hard-coded nav tree in the static app), `SiteFooter` (nav columns, brand blurb, legal, social), `ThemeToggle`.
- **Home composition:** Hero → ValueProps band → Destinations preview grid → Experience showcase → Stats band → Promo band → Testimonials ("Mission Log") → Newsletter CTA.
- **Reusable components:** `Hero`, `ValueProps` (+ `ValueCard`), `DestinationsGrid` (+ `DestinationCard`), `ExperienceShowcase` (+ `ExperienceItem`), `StatsBand` (+ `Stat`), `PromoBand`, `Testimonials` (+ `TestimonialCard`), `NewsletterCTA`, `ContactForm` (presentational), `PageHero` (inner-page header), `RichTextSection` (About story body).
- **Theme stack:** light/dark/system, Tailwind v4 tokens, preflight, provider.
- **Tests:** component/unit (Vitest) for prop rendering + theme provider; Playwright screenshot-diff per route per theme.
- **`sitecore-content-model.md`:** the Act-2 derived spec (mechanical from props).

### Out of Scope

- **→ PRD-001:** head-app scaffold, baseline copy + token replacement, SDK field-component port, component-map registration, component-level navigation GraphQL, live `client.getPage()` data, `/api/layout-debug` probing, Vercel deploy.
- **Deferred entirely (v1):** commerce/cart/checkout, i18n/multilingual, personalization/A-B, real forms backend (email/CRM), site search, blog/CMS-editorial workflows, auth.

## 6. User Stories

**US-1 — Faithful static site (operator/design).** As the operator, I want a plain-React static app that faithfully realizes the approved clickdummy so that design is locked before any Sitecore integration.
- AC1: All 5 routes exist and render with the components in §5.
- AC2: Screenshot-diff vs the selected clickdummy variant meets the fidelity definition (structural parity + ≤~5% pixel delta) at 390 + 1440, light + dark, before I review.
- AC3: No `@sitecore-content-sdk/*` import anywhere in `static/`.

**US-2 — 3-state theming (visitor).** As a visitor, I want light/dark/system theme with my choice persisted and no flash so the site feels polished.
- AC1: Toggle cycles light → dark → system; selection persists across reload (localStorage).
- AC2: Hard reload shows no FOUC (preflight sets the class before paint).
- AC3: No hydration mismatch warning in console.

**US-3 — Sitecore-portable components (build agents).** As the Developer, I want each component's prop boundary designed for a mechanical Act-3 field-component swap.
- AC1: Every **leaf** component's props are a single flat object whose scalars each map to one Sitecore field; every **container/list** component follows the array derivation rule (folder-of-child-items, §10).
- AC2: Each scalar prop has an obvious SDK field type (string→Single-Line, html→Rich Text, image→Image, link→General Link).
- AC3: Header/footer nav is the documented exception (sourced in Act 3, not derived). Any other prop that can't map is explicitly flagged net-new in `sitecore-content-model.md`.

**US-4 — Operator CMS runbook (operator-as-author).** As the operator, I want a dependency-ordered, exact-path checklist so I can build + publish the tenant in Act 2 without guessing.
- AC1: `sitecore-content-model.md` lists site collection/site nodes with exact tree paths.
- AC2: Per-rendering table gives `componentName` (verbatim), datasource template + fields + types, parameters, resolver, variants.
- AC3: Per-page composition + content items with verbatim field values + publish targets are present.

**US-5 — Representative marketing content (visitor).** As a prospective customer, I want compelling cosmos-brand copy and visuals across destinations and experiences so the site reads like a real product.
- AC1: Each route has real (not lorem) on-brand copy.
- AC2: Imagery is royalty-free/placeholder/generated; no copyrighted assets.
- AC3: Motion is tasteful and respects `prefers-reduced-motion`.

## 7. Functional Requirements

- **FR-1** Static app scaffolded via the canonical `create-next-app` (App Router, TS, Tailwind v4) at `products/head-application/aphelion/static/`.
- **FR-2** Five App-Router routes per §5, composed from the reusable component inventory.
- **FR-3** Each component is a typed plain-prop React component (one flat props interface), no Sitecore types.
- **FR-4** Theme provider + toggle + preflight per `custom_content-sdk-theme-hydration-pattern`; tokens in Tailwind v4 `@theme`.
- **FR-5** Responsive: mobile (≤640), tablet, desktop (≥1024) layouts for every route; header collapses to a mobile menu.
- **FR-6** Accessibility baseline: semantic landmarks, keyboard-operable nav + toggle + form, visible focus, alt text, color-contrast AA on both themes.
- **FR-7** Presentational `ContactForm` + `NewsletterCTA`: client-side validation + success/empty/error UI states; no network submit.
- **FR-8** Component tests (render-from-props, theme switching) + Playwright screenshot-diff per route per theme.
- **FR-9** `sitecore-content-model.md` generated per the Phase-5.5 section template (derived mechanically from props).

## 8. Non-Functional Requirements

- **NFR-1 Performance (soft target, non-blocking):** Lighthouse performance ≥ 90 on Home (`next build && next start`, desktop); images sized/lazy; no layout shift from theme init. This is a target, NOT a §12 ship gate — keep perf hygiene but do not block on it (animation tradeoffs + dogfood).
- **NFR-2 Hydration safety:** zero browser-global access in render/SSR-init paths; zero hydration warnings.
- **NFR-3 Motion:** all animation gated by `prefers-reduced-motion`; no autoplay audio/video.
- **NFR-4 Maintainability:** components < ~150 lines where practical; theme + tokens centralized; props flat.
- **NFR-5 Portability:** the static app must be portable to the head app with the theme code moving verbatim and each component's props mapping to fields without restructuring.
- **NFR-6 Accessibility:** WCAG 2.1 AA contrast on both themes (runtime-verified during test).

## 9. Technical Considerations

- **Stack:** Next.js (App Router) + React + Tailwind v4 + TypeScript, plain `create-next-app`. **Zero Sitecore packages** — this is the defining constraint of Act 1.
- **Build mode:** standard Next.js production build (`next build`), **NOT** `output: 'export'`/SSG — a static export would misrepresent the SSR/proxy head-app target the components port into, and `next build && next start` is how NFR-1 perf is measured.
- **Array/container mapping:** array-of-child props map to a **folder-of-child-items** datasource (SXA Children resolver), not a single flat field — see §10 array derivation rule. This is the load-bearing content-modeling decision the dogfood produces.
- **Theme:** `custom_content-sdk-theme-hydration-pattern` is authoritative. `public/theme-init.js` preflight; `useSyncExternalStore` provider (per memory: Next16/React19 react-hooks plugin ERRORS on setState-in-effect for client-only state — use `useSyncExternalStore` for localStorage/clock/window reads). Mounted-flag for client-only styling.
- **Tailwind v4 tokens:** `@theme` block; `@custom-variant dark`. Do NOT wrap hex-literal tokens in `hsl()` (breaks parsing → currentColor/black) — use `var(--token)` directly; alpha via `color-mix`/SVG `stopOpacity`.
- **Prop-design = content-model design:** keep each component's prop boundary one flat object that maps 1:1 to a future datasource. This is the single most load-bearing rule for a mechanical Act-3 port; choose field types now (string→Single-Line Text, html→Rich Text, `<img>`→Image, `<a>`→General Link).
- **Fidelity gate:** Playwright screenshot-diff (per `feedback_poc_is_implementation_contract` — build from the clickdummy markup, not prose). Structural drift is what this catches.
- **Imagery:** placeholder/royalty-free/generated (gradients, starfield SVG/canvas). No copyrighted brand assets. Default demo video (if any) `https://youtu.be/87rKo2T6nMA`.
- **ADR candidates:** (a) static-app-first vs scaffold-first ordering; (b) theme architecture (preflight + useSyncExternalStore); (c) flat-prop / content-model-driven component boundary; (d) two-PRD split (Acts 1–2 vs Act 3); (e) presentational-only forms in v1.

## 10. Data Model Overview

No runtime data store in PRD-000 — content is **static, hard-coded props** in the static app. The *design-time* data model is the **future Sitecore content model**, derived in `sitecore-content-model.md`. Indicative datasource templates (one per content-bearing component):

| Component | Datasource template (future) | Fields (name : SDK field type) |
|-----------|------------------------------|--------------------------------|
| Hero | `Hero` | Eyebrow:Single-Line · Title:Single-Line · Subtitle:Rich Text · Image:Image · PrimaryCta:General Link |
| ValueCard | `Value Prop` | Icon:Single-Line · Title:Single-Line · Body:Rich Text |
| DestinationCard | `Destination` | Name:Single-Line · Tagline:Single-Line · Image:Image · Distance:Single-Line · Detail:Rich Text · Link:General Link |
| ExperienceItem | `Experience` | Title:Single-Line · Summary:Rich Text · Image:Image · Duration:Single-Line · Cta:General Link |
| Stat | `Stat` | Value:Single-Line · Label:Single-Line |
| PromoBand | `Promo` | Heading:Single-Line · Body:Rich Text · Image:Image · Cta:General Link |
| TestimonialCard | `Testimonial` | Quote:Rich Text · Author:Single-Line · Role:Single-Line · Avatar:Image |
| NewsletterCTA | `Newsletter` | Heading:Single-Line · Body:Rich Text · Placeholder:Single-Line · ButtonLabel:Single-Line |
| RichTextSection | `Rich Text Section` | Heading:Single-Line · Body:Rich Text |
| PageHero | `Page Hero` | Title:Single-Line · Subtitle:Rich Text · Image:Image |
| ContactForm *(presentational)* | `Contact Form` | Heading:Single-Line · Intro:Rich Text · NameLabel:Single-Line · EmailLabel:Single-Line · MessageLabel:Single-Line · ButtonLabel:Single-Line *(no submit target — copy/labels only)* |

**Array derivation rule (container/list components).** Components rendering an array of children (`DestinationsGrid`→`Destination[]`, `ValueProps`→`ValueCard[]`, `ExperienceShowcase`→`ExperienceItem[]`, `StatsBand`→`Stat[]`, `Testimonials`→`TestimonialCard[]`) do **not** map to a single flat datasource. They follow the **SXA Children-resolver pattern**: the container's datasource is a **folder item**; each child is an item of the child template above carrying the per-card fields; the container's own scalars (section heading/eyebrow) are fields on the folder item (or rendering parameters). This folder-of-children mapping — not multilist, not a repeating field — is the canonical content-model output of this dogfood (per `custom_content-sdk-resolver-patterns`).

**Nav exception.** `SiteHeader` / `SiteFooter` navigation is **excluded** from content-model derivation. In the static app it is a hard-coded tree (visual only); in Act 3 (PRD-001) it is sourced via the component-level navigation pattern. It deliberately violates the flat-prop rule and must not be flagged.

(The authoritative version — with exact tree paths, parameters, resolvers, variants, publish targets — is produced as `sitecore-content-model.md` in `/implement` Act 2.)

## 11. UX Considerations

- **Tone:** premium, aspirational, calm-futuristic. Generous whitespace, restrained palette anchored on deep-space dark + a vivid accent (`*-500` token for accents per the dark-mode policy), elegant serif display + clean sans body (à la the personal-website Cormorant+Jost lesson — but choose Aphelion's own pairing).
- **Motion:** subtle parallax starfield on hero, fade/slide-in on scroll (reduced-motion safe), hover elevation on cards. Nothing that fights readability.
- **Both themes first-class:** dark = the brand's "night sky" hero state; light = a clean "daybreak" variant. Neither is an afterthought.
- **Responsive:** card grids reflow 3→2→1; header → hamburger on mobile; hero text scales.
- **States:** forms show default/focus/error/success/empty; cards have hover + focus-visible.

## 12. Release Plan

- **PRD-000 (this) = Acts 1–2.** Ship = the static reference app + `sitecore-content-model.md`, committed + pushed to `prd-000`. There is no production deploy here (the static app is a reference deliverable, not deployed). **Ship gates (all must pass):** `npm run build` + `npm run lint` + `npx tsc --noEmit` green · component/unit tests pass · screenshot-diff fidelity gate passes (§1.5 definition) both themes · a11y/contrast AA both themes · `sitecore-content-model.md` complete. **Non-gate:** NFR-1 Lighthouse (soft target).
- **Tranches** (per the always-tranche preference; uncertain items get a test tranche first):
  1. **T-foundation:** scaffold static app + theme stack + chrome (header/footer/toggle) — *uncertainty: theme hydration; verify with Playwright before building content.*
  2. **T-home:** Home route + all home-band components.
  3. **T-inner:** Destinations, Experiences, About, Contact routes + remaining components.
  4. **T-fidelity:** screenshot-diff gate (both themes) + a11y/contrast pass + tests.
  5. **T-spec:** derive `sitecore-content-model.md`.
- **PRD-001 = Act 3** (separate run): tenant build (operator) → head-app scaffold → port → deploy to Vercel.

## 13. Risks and Mitigations

| Risk | Severity | Mitigation |
|------|----------|------------|
| Theme hydration mismatch / FOUC | High | Follow `custom_content-sdk-theme-hydration-pattern` exactly; preflight + useSyncExternalStore; Playwright is the gate (vitest can't catch) |
| Component props that don't map to a single Sitecore field → painful Act-3 port | High | Enforce flat-prop rule at design + review; flag any net-new field explicitly in the spec |
| Design drift from clickdummy (prose-built instead of markup-built) | Med | Screenshot-diff gate BEFORE operator review; build from clickdummy structure |
| Over-animation hurting taste/perf/a11y | Med | `prefers-reduced-motion` gating; performance budget NFR-1; "not overloaded" is explicit |
| Tailwind v4 hex-token-in-`hsl()` breakage | Med | Use `var(--token)` directly; documented in §9 |
| Scope creep toward Act 3 inside PRD-000 | Med | Hard out-of-scope list; no `@sitecore-content-sdk/*` allowed |
| Copyrighted imagery | Low | Royalty-free/placeholder/generated only |

## 14. Open Questions

- **OQ-1** Exact typographic pairing + accent hue for Aphelion — resolved at `/architect` (UI Designer proposes 2 directions; operator picks).
- **OQ-2** Final destination/experience set (names, count) — UI Designer + Developer choose representative content; operator may adjust.
- **OQ-3** Will PRD-001 reuse this repo's `main` (stacked off `prd-000`) — decided at PRD-001 kickoff (expected: yes, branch from `prd-000` until merged).

## 15. Future Opportunities

- Act 3 head-app port + Vercel deploy (PRD-001).
- i18n (next-intl), personalization, real forms backend, search — each a future PRD.
- Editor-mode polish once on the head app.
- Reuse Aphelion as a standing reference for future Content SDK new-site dogfoods.

## Handoff Metadata
- Canonical run manifest: `project-planning/workflow/run-20260609T150924Z.json`
- Source concept brief: none (prompt-only dogfood request, 2026-06-09)
- Recommended next command: `/architect`
- Recommended next input file: `project-planning/architecture/architecture-<timestamp>.md`
